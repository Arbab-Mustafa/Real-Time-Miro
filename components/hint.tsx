import React from "react";
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@components/ui/Tooltip";

interface HintProp {
  label: string;
  children: React.ReactNode;
}

const Hint = () => {
  return <div>Hint</div>;
};

export default Hint;
